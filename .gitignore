/.phpunit.cache
/node_modules
# public/build NO se ignora: la imagen de producción es php:8.2-apache, sin Node,
# así que el servidor no puede compilar. Si el CSS no viaja con el código, el
# deploy sube las vistas nuevas y deja el bundle viejo: las pantallas salen sin
# los estilos que todavía no existen en ese archivo.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Pruebas de navegador (Playwright): rastros, videos y reportes de cada corrida.
/test-results
/playwright-report
/blob-report
/playwright/.cache
